---
import Link from "@/components/Link.astro";

import type { Process } from "@/data/process";

const { step, title, description, action } = Astro.props as Process;
---

<div class="process-card">
  <div class="process-card__step">
    {step}
  </div>
  <header class="process-card__header">
    <h2 class="process-card__title">{title}</h2>
    <p class="process-card__description">
      {description}
    </p>
  </header>
  {
    action && (
      <Link
        variant="flat"
        context="brand"
        href="#plans"
        iconTrailing="arrow-up-right"
      >
        {action}
      </Link>
    )
  }
</div>

<style>
  .process-card {
    max-inline-size: 60rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 1rem 1.25rem;
    background-color: var(--color-background-default);
    border: 1px solid var(--color-border-subtle);
    box-shadow: 0px 1px 2px var(--color-border-subtle);
    border-radius: 0.75rem;
  }

  .process-card__description {
    color: var(--color-foreground-subtle);
  }

  .process-card__header {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .process-card__step {
    display: grid;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 0.5rem;
    background-color: hsl(from var(--color-background-brand) h s l / 8%);
    color: var(--color-foreground-brand);
    font-weight: 500;
  }

  .process-card__title {
    font-size: var(--font-body-size-large);
    font-weight: 600;
  }
</style>
